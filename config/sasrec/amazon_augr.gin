# SASRec config for Amazon dataset
# Self-Attentive Sequential Recommendation
# Run: python genrec/trainers/sasrec_augr_trainer.py config/sasrec/amazon.gin

import genrec.data.amazon_sasrec
import genrec.models.sasrec_augr

# =============================================================================
# Training Settings
# =============================================================================
train.epochs = 300
train.batch_size = 128
train.learning_rate = 1e-3
train.weight_decay = 0.0

# =============================================================================
# Model Settings (matching original paper)
# =============================================================================
train.max_seq_len = 50
train.embed_dim = 64
train.num_heads = 1
train.num_blocks = 2
train.ffn_dim = 256
train.dropout = 0.5
train.loss_type = "ce"  # "bce" (original paper, 1 neg per pos) or "ce" (cross-entropy over all items)
train.ctr_mode="listnet"
train.listnet_scale = 20
train.lambda_ctr = 0.1
train.lambda_gen = 1.0
train.gen_loss_decay = False
train.model_name = "base-listnet-ctr-{split}-no-gen-decay"

# =============================================================================
# Dataset Settings
# =============================================================================
train.dataset_folder = "/home/work/chatbot-llms-3/bin.fu/genrec/genrec/data/dataset/amazon"
train.split = "{split}"  # beauty, sports, toys, clothing

# =============================================================================
# Evaluation Settings
# =============================================================================
train.do_eval = True
train.eval_every_epoch = 1
train.eval_batch_size = 256
train.patience = 50

# =============================================================================
# Checkpoint Settings
# =============================================================================
train.save_dir_root = "out/sasrec/amazon/{split}"
train.save_every_epoch = 50

# =============================================================================
# Logging Settings
# =============================================================================
train.wandb_logging = True
train.wandb_project = "sasrec_augr_{split}"
train.wandb_log_interval = 100

# =============================================================================
# Accelerator Settings
# =============================================================================
train.amp = True
train.mixed_precision_type = "bf16"
